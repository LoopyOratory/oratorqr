services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/generate"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 10s
